package interviewPrepare.uber.oaProblem3;

// 图书馆借书，输入一个operations list，每个operation包含：行为，书名，数量，价格
// 行为有三种：存入，借书，更新
//1. 按照价格存入等量的书到图书馆。
//2，借出一定量的书。同名书优先借出低价书。
//3，更新等量书的价格
public class BorrowBooksFromLibrary {
    //You are building a simple Library Management System that supports three types of operations on books:
    //
    //Each operation is represented as a list of 4 elements
    // [operationType, bookName, quantity, price]

    // Where:
    //
    //operationType:
    //
    //1: Store quantity copies of bookName into the library at price price.
    //
    //2: Borrow quantity copies of bookName. Always borrow the cheapest copies first.
    //
    //3: Update quantity copies of bookName from any price level to the new price price. Always update the cheapest copies first.
    //
    //Each bookName can have multiple entries with different prices.



}
